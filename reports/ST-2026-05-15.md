# ST — Session Transfer — TeInformez 2026-05-15

> **Source session**: TeInformez Quick Wins + Sentry wire + AN-04 marker sync (2026-05-15, after ST-2026-05-15-5 continuation handoff)
> **Continuation of**: ST-2026-05-15-5 (post G-TI-PHP-NEWS-API-WARNINGS) — that one was about Phase A (PHP defensive reads). This ST follows-up after a full session of 5 more items closed.
> **Project scope**: TeInformez (RESTRICT — confirmare prealabilă). No Master-level work touched.

---

## Pre-flight obligatoriu (next session start)

```bash
cd ~/Projects/Master
node mesh/tools/optimise.mjs --phase SessionStart --quiet
node mesh/engine/session-bridge.mjs --action status --project TeInformez
git -C ../TeInformez log --oneline -8
```

Expected: optimise critical=1 (pre-existing SSO_JWT_ISSUER_DRIFT, NOT in TeInformez scope — surface but don't escalate). No active TeInformez pipeline. Git log should show `d673c30` as HEAD.

---

## Current state — ce a landat această sesiune (2026-05-15)

### TeInformez commits (HEAD `d673c30`)

| Commit | Item | Scope |
|---|---|---|
| `d1e1db7` | G-TI-PHP-ANALYTICS-FOREACH-NULL | 1 file, 1+/1- — `(array)($rows ?? [])` guard pe `admin/views/analytics.php:250` |
| `f1a6a8c` | OP-SENTRY [x] + CAS-04 handoff doc | TODO_PERSISTENT + `knowledge/CAS-04-handoff-2026-05-15.md` (141 lines) |
| `3c32913` | TODO marker sync EMAIL-06 + G-TI-PHP-ANALYTICS-FOREACH-NULL | docs |
| `d673c30` | AN-04 [x] marker sync (already shipped in `0446212`) | docs |

### Items closed (5 in session)

1. **G-TI-PHP-ANALYTICS-FOREACH-NULL** — null-coalesce guard on unguarded `$wpdb->get_results()` foreach. Live verified: 80s wait, baseline 148 errors → still 148 (zero new).
2. **EMAIL-06** — user applied SPF Hostico `include:spf.brevo.com`. Verified live via `dig +short TXT teinformez.eu`. Single TXT record correct.
3. **OP-SENTRY** — DSN `https://...@o4511393455734784.ingest.de.sentry.io/4511393459798096` wired in `/var/www/teinformez-repo/frontend/.env.local` + copied to standalone target + rebuild + pm2 restart. 5 envelope POSTs verified via Playwright. Sentry user marked first event as resolved → integration confirmed live.
4. **CAS-04** — handoff doc prepared (`knowledge/CAS-04-handoff-2026-05-15.md`). MA endpoint spec ready; user is working on MA side now.
5. **AN-04** — marker sync; was atomic-bundled with AN-01 in commit `0446212` (2026-05-14). 302→login verification confirms both admin pages registered. Cross-links + drilldowns present.

### Sentry account state

- Org: `fabulosos-srl`
- Project: `javascript-nextjs` (Next.js platform)
- Free tier: 5k errors/month + 10K performance + 50 replays
- DSN public + safe to commit-log (already in `Master/credentials/teinformez.env` gitignored)
- Server-side env: `SENTRY_DSN` set (loaded from `.env.local` next to server.js in standalone build)
- Client-side env: `NEXT_PUBLIC_SENTRY_DSN` inlined at build time
- First smoke event marked resolved by user → dashboard clean

---

## Pending priorities (post-session)

### 🔴 Blocked on MA endpoint dev (user is working on it now)
- **CAS-04** wire newsletter + InFeed la `/api/cas/render` — spec `knowledge/CAS-04-handoff-2026-05-15.md`. Post-MA: ~30min TeInformez side.
- **CAS-05** banner CAS pe homepage `/` — post CAS-04 live.
- **AN-03** Revenue mini-dashboard — post CAS-04 (~1h, uses `${MA_API}/api/cas/metrics`).

### ⏰ Time-gated
- **AN-02-followup** — gate **2026-05-22** (~7 zile post-AN-02 deploy). Verify "Top 5 surse" ratio + query perf.

### 💤 Deferred (require Premium tier active)
- MN-02 Stripe subscriptions, MN-03 Paywall soft, OP-01 churn email, OP-02 onboarding wizard

### Done — nothing else open in TeInformez TODO
All items either `[x]` or blocked on external pre-reqs.

---

## Cross-project pending (NOT TeInformez scope but referenced)

These live in `Master/TODO_PERSISTENT.md`, not TeInformez — listed here only because next session might reference:
- **TRWG-GW sub-bugs** (gateway poll 180s + --review-paths relative resolve) — Master/mesh/engine/trwg-loop.mjs
- **Master tooling** api-tester false-positive + security-scanner phantom innerHTML
- **AIRouter vision support root cause** Phase C/D/E
- **SSO E2E ecosystem-wide verification** dedicated session
- **ML2 Waves 2-5** (4PRO + AVE ecosystems + standalone prod + TradeInvest + local)

---

## Facilities

### Test credentials
- `Master/credentials/teinformez-test-users.env` — `e2e-admin@teinformez.test` / `E2eAdmin2026!`
- WP admin: `alexdanciulescu@gmail.com` / `MihDan74!?><` (rotation recommended, not blocker)

### Infrastructure
- VPS2 (TeInformez): `ssh root@72.62.155.74`
- WP source repo: `/var/www/teinformez-repo/`
- WP serving: `/var/www/teinformez/`
- Frontend repo: `/var/www/teinformez-repo/frontend/`
- Frontend standalone serving: `/var/www/teinformez-frontend/` (PM2 id 17, name `teinformez`, port 3002)
- Logs: `/var/log/nginx/teinformez.error.log` + `/var/log/php8.3-fpm.log`

### Deploy commands
- Backend: `ssh root@72.62.155.74 "/var/www/deploy.sh teinformez"` (git pull + opcache reset via PHP-FPM restart)
- Frontend:
  ```bash
  ssh root@72.62.155.74 "cd /var/www/teinformez-repo && git pull origin master && cd frontend && npm run build && cp -r .next/static .next/standalone/.next/ && cp -r public/ .next/standalone/public/ && rsync -a --delete .next/standalone/ /var/www/teinformez-frontend/ && cp .env.local /var/www/teinformez-frontend/.env.local && pm2 restart teinformez"
  ```
  **IMPORTANT**: cp `.env.local` after rsync (rsync --delete clears it; without copy, Sentry SSR + future env vars won't load)

### Sentry
- Dashboard: https://fabulosos-srl.sentry.io/issues/
- DSN: see `Master/credentials/teinformez.env`
- Project: `javascript-nextjs` (Next.js SDK 10.52.0, session tracking ON, release tagged auto from commit hash)

### Brevo (email transactional)
- Domain `teinformez.eu` fully authenticated: DKIM brevo1+brevo2 + SPF (now includes spf.brevo.com) + brevo-code TXT
- From address: `noreply@teinformez.eu` (real Hostico mailbox, forwards bounces to alexdanciulescu@gmail.com)
- API key: `Master/credentials/teinformez.env` (legacy file or `brevo_api_key` WP option)

---

## Active rules (next-session-relevant)

- **TeInformez = RESTRICT** (CLASSIFICATION §4) — confirmare prealabilă pe modificări (not propose-confirm-apply per edit like NO-TOUCH; confirmation suffices for planned scope)
- **`feedback_no_files_in_master`** — project-scoped artifacts (HANDOFF, walk scripts, screenshots) live în `~/Projects/TeInformez/`, NOT în Master/. Master = cross-project state only. (This ST file is in TeInformez/Reports/.)
- **`feedback_scope_discipline`** — ship exactly what was asked; collateral improvements → separate TODO items (e.g. G-TI-PHP-ANALYTICS-FOREACH-NULL was logged separately when found during Phase A verification)
- **`feedback_verification_ritual`** — re-run the original failing test after fix + deploy; "done" ≠ "commit + HTTP 200". This session: log delta 0/0, dig TXT verify, 5 envelope POSTs verify, 302→login verify
- **`feedback_complete_testing_default`** — /review + TWG after each development; STRICT auto-suspend confirm on "rapid/skip/doar/quick" triggers
- **L82 research-before-proposing** — for any new fix, read source + verify runtime/dependency state BEFORE proposing
- **L124 ST display in chat** — ST prompt MUST be displayed in chat, file = supplement
- **CLAUDE.md §3 session-end ritual** — 3-step (backup + ST + Optimise SessionEnd) + audit-receipt 4-bullet
- **Sentry verify**: include "marked resolved by user" in OP-SENTRY entry — user verified first event arrived 2026-05-15

---

## Notă recovery

If you need to reconstruct what happened in this session, walk commits `d1e1db7..d673c30` (4 commits in TeInformez) + `Master/credentials/teinformez.env` (gitignored, on local Mac + VPS2 source repo and standalone target).

Sentry DSN is the same on all 3 locations: Master/credentials, VPS2 source `.env.local`, VPS2 standalone `.env.local`. Rotating it requires:
1. Generate new DSN in Sentry → Settings → Projects → javascript-nextjs → Client Keys (DSN)
2. Update 3 locations (Master/credentials + VPS source + VPS standalone)
3. Rebuild + redeploy frontend (NEXT_PUBLIC inlined at build)
4. PM2 restart

---

## Next session entry point

If user invokes "continue TeInformez" or comes back with MA endpoint live → enter CAS-04 wire-up flow per `knowledge/CAS-04-handoff-2026-05-15.md` section "Post-MA-endpoint TeInformez wiring (this session next time)".

If user invokes "AN-02-followup" on/after 2026-05-22 → ~15min check on Top 5 sources query.

If session start with no specific request + TeInformez TODO empty of actionable → propose Master cross-project tooling work (TRWG-GW sub-bugs, api-tester false-positive, etc.) or switch project context.
